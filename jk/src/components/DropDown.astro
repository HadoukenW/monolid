---

---


<div class="dropdown__menu">
    <ul>
        <li>
            <a class="dropdown__link" href="/#ourServices">Оценка недвижимости</a>
        </li>
        <li>
            <a class="dropdown__link" href="/#ourServices">Ипотека</a>
        </li>
        <li>
            <a class="dropdown__link" href="/#ourServices">Продажа квартиры</a>
        </li>
        <li>
            <a class="dropdown__link" href="/#ourServices">Рефинансирование</a>
        </li>
        <li>
            <a class="dropdown__link" href="/#ourServices">Аренда</a>
        </li>
    </ul>
</div>

<style lang="scss" is:global>

    .dropdown {
        padding-right: 20px;

        

        @media(max-width: 992px) { 
            display: flex;
            flex-direction: column;
            
        }
        &:hover {
                .prevent {
                    color: var(--btn-primary) !important;    
                }
        }
        .prevent {      
            transition: .3s ease all;  
        }
    }

    .dropdown__menu {
        position: absolute;
        left: 0;
        width: 225%;
        background-color: #03283c;
        padding: 15px 0;
        z-index: 2;
        top: 30px;
        transition: .45s ease all;
        opacity: 0;
        transform: translateY(-20px);
        pointer-events: none;

            &.active {
                opacity: 1;
                transform: translateY(0px);
                pointer-events: all;
            }

        @media(max-width: 992px) {
            position: static;
            opacity: unset;
            transform: unset;
            pointer-events: unset;
            width: 100%;
            background-color: unset;
            padding: unset;
            z-index: 2;
            transition: unset;
            margin-top: 10px;
            display: none;
           &.active {
            display: block;
           } 
        }

        ul {
            display: flex;
            flex-direction: column;
            font-size: 18px;
            width: 100%;

            @media(max-width: 992px) {
                align-items: center;
                
                
            }

            li {
                width: 100%;
                display: flex;
            }
        }
        a {
            font-size: 16px;
            color: var(--primary);
            padding: 20px 10px;
            width: 100%;
            transition: .3s ease all;
            &:hover {
                background-color: var(--primary);
                color: var(--headers-primary);
            }

            @media(max-width: 992px) {
                color: var(--headers-primary);
                font-size: 18px;  
                padding: 10px;
            }
        }

        .dropdown__link {
            transition: .3s ease color;
            &:hover {
                color: var(--btn-primary);
            }
        }
    }
</style>